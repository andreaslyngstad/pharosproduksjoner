
	
		<ul><li>Kategorier
		<ul>
		<% @all_categories.each do |cat| -%>
		<li><%= link_to_unless_current(cat.name, :action => 'category', :id => cat.id) %></li>
		<% end -%></li></ul>
	</ul>
	<ul>
		<li>Alle innlegg<ul>
		<% for post in @posts_til_liste -%>
		
		<li><%= link_to_unless_current(h(post.title), :controller => "public", :action => "view_post", :id => post.id) %></li>
		<%end -%></li></ul>
		</ul>
	


	Søk i bloggen
	<% form_tag view_blogg_path, :method => 'get' do %>
	 	<%= text_field_tag :search, params[:search] %>
	    <%= submit_tag "Søk", :name => nil, :class => 'submit' %>
	<% end %>			

